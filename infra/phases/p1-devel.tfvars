# P1 - Red Cell harness + prompt development.  21 h on 1x RTX PRO 6000 (spot).
# Spot is fine here: preemption costs you a restart, not an exercise. Runs the
# Int4 checkpoint, which is also the emergency single-GPU fallback, so that
# config gets 21 hours of real use before anyone has to trust it.
phase             = "p1"
planned_hours     = 21
spend_to_date_eur = 2.46
